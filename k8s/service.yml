apiVersion: v1
kind: Service
metadata:
  name: hotstar-service
spec:
  type: NodePort
  selector:
    app: hotstar
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30007
